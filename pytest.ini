[pytest]
testpaths = tests
pythonpath = tests .
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    integration: marks tests that invoke external processes (deselect with -m 'not integration')
